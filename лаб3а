#include <iostream>
#include <cmath>
using namespace std;

int main() {
	setlocale(LC_ALL, "Ukrainian");
    int a, b, c;
    cout << "����i�� ����i�i���� a, b, c: ";
    cin >> a >> b >> c;

    if (!cin) {
        cout << "�������: ������� �� �i�� �����.\n";
        return 0;
    }

    if (a == 0) {
        cout << "�i������ �� � ����������.\n";
        return 0;
    }

    double D = b * b - 4 * a * c;

    cout << "�������i����: " << D << endl;

    if (D < 0) {
        cout << "�����i� ����.\n";
    }
    else if (D == 0) {
        double x = -b / (2.0 * a);
        cout << "���� ���i��: " << x << endl;
    }
    else {
        double x1 = (-b + sqrt(D)) / (2.0 * a);
        double x2 = (-b - sqrt(D)) / (2.0 * a);
        cout << "��� �����i: " << x1 << " i " << x2 << endl;
    }

    return 0;
}

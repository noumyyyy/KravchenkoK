#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main() {
	setlocale(LC_ALL, "Ukrainian");
    srand(time(0));
    int secret = rand() % 100 + 1; //�������� ����� 1�100

    int guess, tries = 0;

    cout << "����'���� ������� ����� �� 1 �� 100. ��������� ��������!\n";

    do {
        cout << "��� ������: ";
        cin >> guess;

        if (!cin) {
            cin.clear();
            cin.ignore(1000, '\n');
            cout << "�������: ����i�� ���� �����!\n";
            continue;
        }

        tries++;

        if (guess > secret) cout << "�����!\n";
        else if (guess < secret) cout << "�i����!\n";
        else cout << "�i���! �� �i������� �� " << tries << " �����!\n";

    } while (guess != secret);

    return 0;
}
